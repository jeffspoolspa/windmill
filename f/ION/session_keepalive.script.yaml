summary: Keep the injected ION session warm without a browser (raw HTTP)
description: GETs an ION page with the cached cookies to reset ION's inactivity
  timer and bumps session_cache.expiresAt so getOrRefreshSession never launches
  chromium. Runs on the DEFAULT worker (pure HTTP). Pairs with an external login
  minter.
lock: '!inline f/ION/session_keepalive.script.lock'
kind: script
schema: null
