summary: 'Permanent ad-hoc ION runner. Override main() body for one-off ION
  tasks; run via runScriptByPath -> getJob. CURRENT: dump live task config
  (invoiceType/itemCost/serviceType) for closed tasks to compare vs our stored
  config (stale-config check).'
description: ''
lock: '!inline f/ION/_run/adhoc.script.lock'
has_on_behalf_of: false
kind: script
schema: null
tag: chromium
