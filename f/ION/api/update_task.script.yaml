summary: 'ION API (WRITE): edit one task. dry_run first; expect_days guards
  against a stale picture.'
description: 'Adds optional expect_days: the per-day tech map the caller
  believes ION holds. On any disagreement the write is REFUSED rather than
  corrupting a complete-week write.'
lock: '!inline f/ION/api/update_task.script.lock'
kind: script
schema: null
tag: chromium
