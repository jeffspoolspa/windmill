summary: End-to-end pipeline test (fetch -> parse -> normalize -> upsert)
description: Full ingest pipeline. Reuses captured IonSession to skip Chromium
  login. Writes to maintenance.visits / chem_readings / consumables_usage and
  auto-creates pools.
lock: '!inline f/ION/_discover/parse_normalize_test.script.lock'
has_on_behalf_of: false
kind: script
schema: null
