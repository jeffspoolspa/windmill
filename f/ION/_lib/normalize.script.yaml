summary: Normalize parser-output ION dicts to canonical-shaped dicts (with
  non-serviceable detection)
description: Layer 2 of the ION ingest pipeline. Reads field mappings from
  app_config, applies transforms, flags non-serviceable (zero-duration) logs via
  is_serviceable, tracks unmapped fields.
lock: '!inline f/ION/_lib/normalize.script.lock'
has_on_behalf_of: false
kind: script
schema: null
