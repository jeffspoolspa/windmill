summary: ION canonical upsert (per-log grain, ion_task_id + is_serviceable,
  4-tier sl resolver)
description: 'v9: adds name-drift tolerance (fuzzy name at shared addresses) +
  unique-name fallback (address typos) + Address1-as-name (SJC-style contact) so
  ION report identity drift vs QBO-synced records no longer drops visits.'
lock: '!inline f/ION/_lib/upsert.script.lock'
has_on_behalf_of: false
kind: script
schema: null
