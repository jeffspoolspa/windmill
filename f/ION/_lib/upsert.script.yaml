summary: ION canonical upsert (per-log grain; 4-tier sl resolver; closed-task
  date-window attribution)
description: 'v10: attributes visits to CLOSED tasks too (ION bills every
  completed log; date-window picks the right era). 4-tier service_location
  resolver. ion_task_id + is_serviceable.'
lock: '!inline f/ION/_lib/upsert.script.lock'
has_on_behalf_of: false
kind: script
schema: null
