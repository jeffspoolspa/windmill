summary: Schedule day/tech derivation. Retires days ION dropped (no longer gated
  on full_reconcile).
description: A day ION no longer serves is deactivated on every run.
  Forward-only reconciliation left ghost slots active, which corrupt
  complete-week write-back.
lock: '!inline f/ION/_lib/upsert_schedules.script.lock'
kind: script
schema: null
