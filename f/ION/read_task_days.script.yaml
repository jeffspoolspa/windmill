summary: 'READ-ONLY: per-day tech + ServiceRepeat + StartsOn for many tasks'
description: Cadence travels with the days — dropping it caused a wrong-path write.
lock: '!inline f/ION/read_task_days.script.lock'
kind: script
schema: null
tag: chromium
