summary: 'QBO inbox drainer (ADR 008 stream loop): one inbox per system, entity
  dispatch inside'
description: claims billing.qbo_inbox (priority order, 3-attempt dead-letter,
  Invoice supersession) and reflects each entity via the per-entity refresh
  handlers in-process
lock: '!inline f/qbo/drain_qbo_inbox.script.lock'
concurrency_key: qbo_api
concurrency_time_window_s: 10
concurrent_limit: 1
has_on_behalf_of: false
kind: script
schema: null
