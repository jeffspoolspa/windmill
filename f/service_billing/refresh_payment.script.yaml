summary: Optional qbo_body + OCC guard + did_write return
description: Adds qbo_body=None so cdc_reconciler can call in-process. OCC guard
  prevents stale writes from clobbering newer cache. CCTransId verification +
  linked-invoice rechecks still run regardless of did_write.
lock: '!inline f/service_billing/refresh_payment.script.lock'
has_on_behalf_of: false
kind: script
schema: null
