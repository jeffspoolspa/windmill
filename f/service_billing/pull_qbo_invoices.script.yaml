summary: Pull QBO invoices into billing.invoices + match WOs (bulk or single-WO mode)
description: "Default: bulk mode — pulls all invoices for billable WOs that are
  missing or stale. Single-WO mode (pass wo_number): fetches just that WO's
  invoice + auto-chains to pre_process_invoice with force=True."
lock: '!inline f/service_billing/pull_qbo_invoices.script.lock'
kind: script
schema: null
