summary: Drop bogus Invoice expectations + safety net for orphaned pending rows
description: Removed Invoice webhook expectation INSERT after
  refresh_invoice_cache in both charge path and orphan recovery — QBO does NOT
  fire Invoice.Update webhooks for payment-driven balance changes. Added
  top-level try/except around _process_invoice_only and _process_charge_path so
  uncaught exceptions flip the attempt to charge_uncertain instead of leaving it
  pending forever.
lock: '!inline f/service_billing/process_invoice.script.lock'
has_on_behalf_of: false
kind: script
schema: null
