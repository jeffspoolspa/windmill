summary: 'Ledger-healing probe: discover + enqueue missing balance inputs'
description: walks mismatched invoices LinkedTxn, enqueues missing
  Payments/CreditMemos (source=probe), logs unmodeled txn types to drift_log as
  the entity-class evidence
lock: '!inline f/service_billing/probe_balance_integrity.script.lock'
concurrency_key: qbo_api
concurrency_time_window_s: 10
concurrent_limit: 1
has_on_behalf_of: false
kind: script
schema: null
