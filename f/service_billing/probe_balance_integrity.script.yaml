summary: Ledger-healing probe (+ reverse diff for stale applications)
description: missing inputs enqueued AND cache-side applications the leader no
  longer lists (dead payments 404 -> mirror delete)
lock: '!inline f/service_billing/probe_balance_integrity.script.lock'
concurrency_key: qbo_api
concurrency_time_window_s: 10
concurrent_limit: 1
kind: script
schema: null
