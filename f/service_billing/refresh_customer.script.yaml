summary: Single-customer QBO refresh — propagates display_name renames to invoices
description: 'Webhook-driven cache refresh script. Called by /api/webhooks/qbo
  when QBO fires the matching entity event. Idempotent: safe to call multiple
  times for the same entity (re-deliveries from Intuit, manual retries,
  reconciler healing). Returns a status object the webhook handler logs to
  billing.webhook_log.'
lock: '!inline f/service_billing/refresh_customer.script.lock'
concurrency_time_window_s: 0
concurrent_limit: 5
has_on_behalf_of: false
kind: script
schema: null
