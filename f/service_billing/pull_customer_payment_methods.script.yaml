summary: Refresh cached QBO payment methods (token via the ADR 012 one door)
description: 'refresh_qbo_token now delegates to f/qbo/get_access_token (cache
  in front of f/qbo/api/get_access_token, concurrent_limit=1) instead of POSTing
  to Intuit itself. Measured: 115 pairs of token refreshes across QBO scripts
  began within 1s of each other over 3 days; simultaneous refreshes race the
  rotating refresh token.'
lock: '!inline f/service_billing/pull_customer_payment_methods.script.lock'
kind: script
schema: null
