summary: Refresh cached QBO payment methods (token via the ADR 012 one door)
description: refresh_qbo_token delegates to f/qbo/get_access_token. Returns the
  token ALONE — this caller does not unpack a tuple.
lock: '!inline f/service_billing/pull_customer_payment_methods.script.lock'
kind: script
schema: null
