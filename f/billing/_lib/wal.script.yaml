summary: Shared WAL over billing.processing_attempts + charge_attempted emission
  (ADR 010)
description: create_attempt emits charge_attempted in the write-ahead
  transaction (real intents only — no dry runs, no sibling stubs).
lock: '!inline f/billing/_lib/wal.script.lock'
kind: script
schema: null
