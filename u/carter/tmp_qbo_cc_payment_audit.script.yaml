summary: 'TEMP (delete after run): QBO payments marked credit-card method but
  never actually charged through QBO Payments'
description: Throwaway audit script created by Claude at Carter's request. Pulls
  all QBO Payments, identifies those whose PaymentMethodRef is a
  CREDIT_CARD-type method but which lack
  CreditCardPayment.CreditChargeResponse.CCTransId / ProcessPayment evidence of
  a real gateway charge. Delete after retrieving results.
lock: '!inline u/carter/tmp_qbo_cc_payment_audit.script.lock'
kind: script
schema: null
